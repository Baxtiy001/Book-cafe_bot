# ============================================================
# Book Cafe — Telegram Mini App ecosystem configuration
# Copy these lines into a `.env` file next to bot.py and fill in.
# ============================================================

# --- Telegram (REQUIRED) ------------------------------------
# Get from @BotFather
BOT_TOKEN=123456789:AAF...your-bot-token-here
# Bot username WITHOUT @ (used for deep links)
BOT_USERNAME=my_bookcafe_bot

# --- Access control ------------------------------------------
# Comma-separated Telegram numeric ids with full admin power.
# Get your id: send /myid to your bot after first run (or use @userinfobot).
ADMIN_IDS=6123456789

# --- URLs -----------------------------------------------------
# Public HTTPS base URL (use ngrok for local testing, see README step 5).
BASE_URL=https://your-subdomain.ngrok-free.app
# Mini App URL. Telegram REQUIRES https. Usually BASE_URL + /webapp
WEBAPP_URL=https://your-subdomain.ngrok-free.app/webapp/

# --- Server ---------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8080
USE_WEBHOOK=true
SECRET_PATH=telegram-webhook
WEBHOOK_SECRET=change-me-to-a-long-random-string

# --- Storage --------------------------------------------------
DB_PATH=data/bookcafe.db
MEDIA_DIR=data/media

# --- Integrations (1C / 1UZ) ----------------------------------
# Shared token that accounting systems must send as X-Integration-Token
INTEGRATION_TOKEN=change-me-integration-token
INTEGRATION_TIMEOUT=15

# --- Misc ------------------------------------------------------
DEFAULT_LANG=uz
SUPPORT_URL=https://t.me/your_support
